# 爬取数据
## 操作
### 1.下载requests库
在python中输入'pip install requests'
### 2.引用requests
```
import requests
headers={
"User-Agent":"此处需要打开任意网页 右键检查 选择网络 打开一个访问成功的去寻找Uesr-Agent并粘贴"
}
responce=requests.get("所需爬取网页网址"，headers=headers)
```
